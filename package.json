{
  "name": "first-npm-package",
  "version": "1.0.0",
  "description": "First npm package",
  "main": "index.js",
  "scripts": {
    "node-version": "node -v",
    "start": "node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Baglan",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prettier": "3.0.3"
  }
}
